# Implementation Guide
1. Setup DB Schema (Run SQL in `database/`).
2. Deploy Edge Function (`edge-functions/create-sub-account`).
3. Integrate Frontend Providers (`frontend/auth/`).
4. Configure Routes using `ProtectedRoute.tsx`.
